import { useState, useEffect, useRef } from "react";
import * as db from "./db";
import { signOut } from "./AuthGate";

const LOGO_SRC = "data:image/png;base64,";

const num = (v) => { const n = parseFloat(v); return isNaN(n) ? null : n; };
const money = (n) => (n == null || isNaN(n)) ? "—" : "$" + Number(n).toFixed(2);

const STYLE = `
@import url('https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@500;600;700&family=Inter:wght@400;500;600&display=swap');
.pm * { box-sizing:border-box; }
.pm { --ink:#101012; --paper:#FBFAF5; --line:#E6E1D6; --muted:#71757E; --chili:#E0392B; --yellow:#FFCE1F; --teal:#0E7C6B;
  font-family:'Inter',system-ui,sans-serif; color:#191B1F; background:var(--paper); min-height:100vh; line-height:1.45; }
.pm .cond { font-family:'Barlow Condensed',sans-serif; }
.pm .fig { font-variant-numeric:tabular-nums; }
.pm .rail { background:var(--ink); color:#F5F2EA; padding:16px 20px; display:flex; justify-content:space-between; align-items:center; gap:14px; flex-wrap:wrap; }
.pm .rail img { width:50px; height:50px; border-radius:50%; }
.pm .brandk { font-family:'Barlow Condensed',sans-serif; font-weight:700; text-transform:uppercase; letter-spacing:.05em; font-size:22px; line-height:1; }
.pm .eyebrow { font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; letter-spacing:.16em; font-size:11px; color:#9aa0a8; font-weight:600; }
.pm .checker { height:9px; background-color:#fff; background-image:linear-gradient(45deg,#0c0c0e 25%,transparent 25%),linear-gradient(-45deg,#0c0c0e 25%,transparent 25%),linear-gradient(45deg,transparent 75%,#0c0c0e 75%),linear-gradient(-45deg,transparent 75%,#0c0c0e 75%); background-size:18px 18px; background-position:0 0,0 9px,9px -9px,-9px 0; }
.pm .tabs { background:#1b1c20; display:flex; gap:4px; padding:0 16px; overflow-x:auto; }
.pm .tab { font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; letter-spacing:.07em; font-weight:600; font-size:15px; color:#A9AEB8; background:none; border:none; padding:13px 14px; cursor:pointer; border-bottom:3px solid transparent; white-space:nowrap; }
.pm .tab.active { color:#fff; border-bottom-color:var(--yellow); }
.pm .wrap { max-width:1040px; margin:0 auto; padding:20px; }
.pm input, .pm select { font-family:inherit; font-size:14px; border:1.5px solid var(--line); background:#fff; border-radius:8px; padding:9px 11px; color:#191B1F; outline:none; }
.pm input:focus, .pm select:focus { border-color:var(--ink); }
.pm .btn { font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; letter-spacing:.05em; font-weight:600; font-size:15px; border:none; border-radius:9px; padding:11px 16px; cursor:pointer; display:inline-flex; align-items:center; gap:8px; }
.pm .btn-primary { background:var(--chili); color:#fff; }
.pm .btn-ghost { background:#fff; border:1.5px solid var(--line); color:#191B1F; }
.pm .mini { font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; font-size:13px; font-weight:600; border:1.5px solid var(--line); background:#fff; border-radius:7px; padding:6px 11px; cursor:pointer; color:#191B1F; }
.pm .mini-danger:hover { border-color:var(--chili); color:var(--chili); }
.pm .toolbar { display:flex; gap:10px; flex-wrap:wrap; align-items:center; margin-bottom:16px; }
.pm .grow { flex:1; min-width:160px; }
.pm .grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(300px,1fr)); gap:12px; }
.pm .card { background:#fff; border:1.5px solid var(--line); border-radius:12px; padding:14px; }
.pm .card-name { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:20px; line-height:1.05; }
.pm .tag { font-size:10px; text-transform:uppercase; letter-spacing:.1em; color:var(--muted); font-weight:600; }
.pm .units { margin:11px 0 6px; display:grid; grid-template-columns:1fr 1fr 1fr; gap:1px; background:var(--line); border:1px solid var(--line); border-radius:8px; overflow:hidden; }
.pm .ucell { background:#fff; padding:7px 8px; }
.pm .ulabel { font-size:9px; text-transform:uppercase; letter-spacing:.1em; color:var(--muted); font-weight:600; }
.pm .uval { font-size:12.5px; font-weight:600; margin-top:2px; }
.pm .overlay { position:fixed; inset:0; background:rgba(20,20,24,.45); z-index:20; display:flex; justify-content:flex-end; }
.pm .sheet { width:min(520px,100%); background:var(--paper); height:100%; overflow-y:auto; padding:22px; }
.pm .sheet h2 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:25px; text-transform:uppercase; margin:0 0 10px; }
.pm .group { background:#fff; border:1.5px solid var(--line); border-radius:11px; padding:14px; margin-top:14px; }
.pm .group-t { font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; letter-spacing:.1em; font-size:13px; font-weight:700; color:var(--chili); margin-bottom:10px; }
.pm .frow { display:flex; gap:10px; margin-bottom:10px; }
.pm .frow:last-child { margin-bottom:0; }
.pm .field { flex:1; display:flex; flex-direction:column; gap:4px; }
.pm .field label { font-size:11px; text-transform:uppercase; letter-spacing:.07em; color:var(--muted); font-weight:600; }
.pm .field input, .pm .field select { width:100%; }
.pm .vrow { display:flex; gap:8px; align-items:center; margin-bottom:8px; }
.pm table.tbl { width:100%; border-collapse:collapse; font-size:13px; background:#fff; border:1.5px solid var(--line); border-radius:11px; overflow:hidden; }
.pm .tbl th { text-align:left; font-size:10px; text-transform:uppercase; letter-spacing:.07em; color:var(--muted); padding:9px 11px; border-bottom:1.5px solid var(--line); background:#FAF8F2; }
.pm .tbl td { padding:9px 11px; border-bottom:1px solid var(--line); vertical-align:top; }
.pm .vgroup { background:#fff; border:1.5px solid var(--line); border-radius:11px; padding:4px 14px 10px; margin-bottom:14px; }
.pm .vgroup-h { display:flex; justify-content:space-between; align-items:center; padding:11px 0; border-bottom:1.5px solid var(--line); }
.pm .vname { font-family:'Barlow Condensed',sans-serif; font-weight:700; text-transform:uppercase; font-size:18px; }
.pm .crow { display:grid; grid-template-columns:1fr 80px 80px 92px; gap:10px; align-items:center; padding:11px 0; border-bottom:1px dashed var(--line); }
.pm .crow:last-child { border-bottom:none; }
.pm .crow label { display:flex; flex-direction:column; gap:3px; font-size:9px; text-transform:uppercase; color:var(--muted); font-weight:600; }
.pm .ctotal { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:19px; text-align:right; }
.pm .stat { font-size:11.5px; color:var(--muted); margin-top:2px; }
.pm .empty { text-align:center; padding:50px 20px; color:var(--muted); }
.pm .note { background:#FFF8E1; border:1.5px solid #E8C24D; border-radius:9px; padding:11px 13px; font-size:13px; color:#6b5310; }
.pm .ok { background:#E6F4F0; border:1.5px solid var(--teal); color:#0a5c50; border-radius:9px; padding:11px; font-size:13px; margin-bottom:12px; }
.pm .err { background:#FDECE7; border:1.5px solid var(--chili); color:#9a2c12; border-radius:9px; padding:11px; font-size:13px; margin-bottom:12px; }
.pm .bchip { display:inline-flex; align-items:center; gap:5px; background:#F1EEE6; border:1px solid var(--line); border-radius:6px; padding:3px 8px; font-size:12px; margin:0 5px 5px 0; }
.pm .spin { display:inline-block; width:14px; height:14px; border:2.5px solid rgba(255,255,255,.4); border-top-color:#fff; border-radius:50%; animation:s .7s linear infinite; }
@keyframes s { to { transform:rotate(360deg); } }
.pm .tiles { display:grid; grid-template-columns:repeat(auto-fit,minmax(175px,1fr)); gap:12px; margin-bottom:8px; }
.pm .tile { background:#fff; border:1.5px solid var(--line); border-radius:12px; padding:14px 16px; }
.pm .tile.dark { background:var(--ink); color:#F5F2EA; border-color:var(--ink); }
.pm .tile .k { font-size:10px; text-transform:uppercase; letter-spacing:.1em; font-weight:600; color:var(--muted); }
.pm .tile.dark .k { color:#9aa0a8; }
.pm .tile .v { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:30px; line-height:1.05; margin-top:3px; }
.pm .tile .sub { font-size:11.5px; color:var(--muted); margin-top:2px; }
.pm .tile.dark .sub { color:#9aa0a8; }
.pm .panel { background:#fff; border:1.5px solid var(--line); border-radius:11px; margin-bottom:10px; overflow:hidden; }
.pm .panel-h { display:flex; justify-content:space-between; align-items:center; padding:13px 16px; cursor:pointer; font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; letter-spacing:.04em; font-weight:600; font-size:16px; }
.pm .panel-h:hover { background:#FAF8F2; }
.pm .panel-b { padding:6px 16px 16px; border-top:1.5px solid var(--line); }
.pm .bar { height:9px; background:#EFEBE1; border-radius:5px; overflow:hidden; }
.pm .bar > span { display:block; height:100%; background:var(--chili); }
.pm .secthead { font-family:'Barlow Condensed',sans-serif; text-transform:uppercase; letter-spacing:.08em; font-weight:700; font-size:13px; color:var(--muted); margin:20px 0 9px; }
`;

export default function App() {
  const [tab, setTab] = useState("catalog");
  const [products, setProducts] = useState([]);
  const [locations, setLocations] = useState([]);
  const [vendors, setVendors] = useState([]);
  const [units, setUnits] = useState([]);
  const [onhand, setOnhand] = useState({});
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  async function reload() {
    setError("");
    try {
      const [p, l, v, u, oh] = await Promise.all([
        db.getCatalog(), db.listLocations(), db.listVendors(), db.listStorageUnits(), db.onHandByLocation(),
      ]);
      const locName = Object.fromEntries(l.map((x) => [x.location_id, x.name]));
      const latest = {};
      for (const r of oh) {
        const k = r.product_id + "|" + r.location_id;
        const t = r.counted_at ? new Date(r.counted_at).getTime() : 0;
        if (!latest[k] || t >= latest[k].t) latest[k] = { product_id: r.product_id, location_id: r.location_id, qty: Number(r.qty), t };
      }
      const map = {};
      for (const r of Object.values(latest)) {
        const m = map[r.product_id] || (map[r.product_id] = { byLoc: {}, total: 0 });
        m.byLoc[locName[r.location_id] || "?"] = r.qty;
        m.total += r.qty;
      }
      setProducts(p); setLocations(l); setVendors(v); setUnits(u); setOnhand(map);
    } catch (e) {
      setError("Couldn't load data: " + (e.message || e));
    } finally {
      setLoading(false);
    }
  }
  useEffect(() => { reload(); }, []);

  if (loading) return <div className="pm"><style>{STYLE}</style><div className="wrap" style={{ padding: 60, textAlign: "center", color: "#71757E" }}>Loading…</div></div>;

  const tabs = [["catalog", "Catalog"], ["count", "Count"], ["receive", "Receive"], ["shopping", "Shopping"], ["reports", "Dashboard"]];

  return (
    <div className="pm">
      <style>{STYLE}</style>
      <div className="rail">
        <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
          <img src={LOGO_SRC} alt="LVMGP" />
          <div>
            <div className="eyebrow">Kitchen &amp; Concessions Inventory</div>
            <div className="brandk">Las Vegas Mini Grand Prix</div>
          </div>
        </div>
        <button className="mini" onClick={signOut}>Sign out</button>
      </div>
      <div className="checker" />
      <div className="tabs">{tabs.map(([k, l]) => <button key={k} className={"tab" + (tab === k ? " active" : "")} onClick={() => setTab(k)}>{l}</button>)}</div>

      <div className="wrap">
        {error && <div className="err">{error}</div>}
        {tab === "catalog" && <Catalog products={products} vendors={vendors} locations={locations} units={units} onhand={onhand} reload={reload} />}
        {tab === "count" && <Count products={products} locations={locations} onhand={onhand} reload={reload} />}
        {tab === "receive" && <Receive products={products} vendors={vendors} locations={locations} reload={reload} />}
        {tab === "shopping" && <Shopping products={products} vendors={vendors} onhand={onhand} />}
        {tab === "reports" && <Dashboard products={products} onhand={onhand} vendors={vendors} reload={reload} />}
      </div>
    </div>
  );
}

function blankProduct() {
  return { product_id: null, name: "", category: "", brand: "", supc: "",
    purchase_unit: "Case", pack: 1, size: null, size_unit: "",
    count_unit: "each", count_per_case: 1, use_unit: "", use_per_count: null, par_level: null, image_url: null,
    barcodes: [], vendors: [], locations: [] };
}

function Catalog({ products, vendors, locations, units, onhand, reload }) {
  const [q, setQ] = useState("");
  const [edit, setEdit] = useState(null);
  const [finding, setFinding] = useState(false);
  const list = products.filter((p) => !q || p.name.toLowerCase().includes(q.toLowerCase()) || (p.brand || "").toLowerCase().includes(q.toLowerCase()));

  return (
    <div>
      <div className="toolbar">
        <input className="grow" placeholder="Search items…" value={q} onChange={(e) => setQ(e.target.value)} />
        <button className="mini" onClick={() => setFinding(true)}>📷 Scan</button>
        <button className="btn btn-primary" onClick={() => setEdit(blankProduct())}>+ Add product</button>
      </div>
      {list.length === 0 ? <div className="empty">No items yet. Add your first product.</div> : (
        <div className="grid">
          {list.map((p) => (
            <div className="card" key={p.product_id}>
              {p.image_url && <img src={p.image_url} alt="" style={{ width: "100%", height: 120, objectFit: "cover", borderRadius: 8, marginBottom: 8 }} />}
              <div className="tag">{p.category || "Uncategorized"}</div>
              <div className="card-name">{p.name}</div>
              {p.brand && <div className="stat">{p.brand}{p.supc ? ` · #${p.supc}` : ""}</div>}
              <div className="stat">📍 {(p.locations || []).map((l) => l.name + (l.unit_code ? ` ${l.unit_code}` : "")).join(" · ") || "No location"} · on hand {onhand[p.product_id]?.total ?? 0} {p.count_unit}</div>
              <div className="units">
                <div className="ucell"><div className="ulabel">Buy</div><div className="uval fig">{p.purchase_unit}{p.pack > 1 ? ` ${p.pack}×` : " "}{p.size}{p.size_unit ? ` ${p.size_unit}` : ""}</div></div>
                <div className="ucell"><div className="ulabel">Count</div><div className="uval">{p.count_unit} · {p.count_per_case}/case</div></div>
                <div className="ucell"><div className="ulabel">Use</div><div className="uval">{p.use_unit || "—"}</div></div>
              </div>
              <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                <button className="mini" onClick={() => setEdit(JSON.parse(JSON.stringify(p)))}>Edit</button>
                <button className="mini mini-danger" onClick={async () => { if (confirm(`Remove ${p.name}?`)) { await db.deleteProduct(p.product_id); reload(); } }}>Remove</button>
              </div>
            </div>
          ))}
        </div>
      )}
      {edit && <Editor product={edit} vendors={vendors} locations={locations} units={units} onClose={() => setEdit(null)} onSaved={() => { setEdit(null); reload(); }} />}
      {finding && <Finder products={products} onClose={() => setFinding(false)} onFound={(p) => { setQ(p.name); setFinding(false); }} />}
    </div>
  );
}

function Editor({ product, vendors, locations, units, onClose, onSaved }) {
  const [p, setP] = useState(product);
  const [busy, setBusy] = useState(false);
  const [bc, setBc] = useState("");
  const set = (k, v) => setP((s) => ({ ...s, [k]: v }));
  const isNew = !p.product_id;

  function addVendor() {
    const v = vendors.find((x) => !p.vendors.some((pv) => pv.vendor_id === x.vendor_id));
    if (v) setP((s) => ({ ...s, vendors: [...s.vendors, { vendor_id: v.vendor_id, name: v.name, price: null, primary: s.vendors.length === 0 }] }));
  }
  function addLoc() {
    const l = locations.find((x) => !p.locations.some((pl) => pl.location_id === x.location_id));
    if (l) setP((s) => ({ ...s, locations: [...s.locations, { location_id: l.location_id, name: l.name, primary: s.locations.length === 0 }] }));
  }

  async function uploadPhoto(e) {
    const f = e.target.files?.[0]; if (!f) return;
    setBusy(true);
    try { const url = await db.uploadProductImage(f, p.product_id); setP((s) => ({ ...s, image_url: url })); }
    catch (err) { alert("Photo upload failed: " + (err.message || err)); }
    finally { setBusy(false); e.target.value = ""; }
  }

  async function save() {
    setBusy(true);
    try {
      if (isNew) await db.createProduct(p); else await db.updateProduct(p);
      onSaved();
    } catch (e) { alert("Save failed: " + (e.message || e)); setBusy(false); }
  }

  return (
    <div className="overlay" onClick={onClose}>
      <div className="sheet" onClick={(e) => e.stopPropagation()}>
        <h2>{isNew ? "New product" : p.name}</h2>
        <div className="group">
          <div className="group-t">Photo</div>
          <div style={{ display: "flex", gap: 12, alignItems: "center" }}>
            {p.image_url
              ? <img src={p.image_url} alt="" style={{ width: 84, height: 84, objectFit: "cover", borderRadius: 8 }} />
              : <div style={{ width: 84, height: 84, borderRadius: 8, background: "#F0F1F3", display: "flex", alignItems: "center", justifyContent: "center", color: "#B7BBC4", fontSize: 28 }}>📷</div>}
            <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
              <label className="mini" style={{ cursor: "pointer" }}>{busy ? "Working…" : (p.image_url ? "Change photo" : "Add photo")}<input type="file" accept="image/*" capture="environment" style={{ display: "none" }} onChange={uploadPhoto} /></label>
              {p.image_url && <button className="mini mini-danger" onClick={() => set("image_url", null)}>Remove</button>}
            </div>
          </div>
        </div>
        <div className="group">
          <div className="group-t">Identity</div>
          <div className="frow"><div className="field"><label>Name</label><input value={p.name} onChange={(e) => set("name", e.target.value)} /></div></div>
          <div className="frow">
            <div className="field"><label>Category</label><input value={p.category || ""} onChange={(e) => set("category", e.target.value)} /></div>
            <div className="field"><label>Brand</label><input value={p.brand || ""} onChange={(e) => set("brand", e.target.value)} /></div>
          </div>
          <div className="frow"><div className="field"><label>SUPC / Item #</label><input value={p.supc || ""} onChange={(e) => set("supc", e.target.value)} /></div></div>
          <div className="field" style={{ marginTop: 8 }}>
            <label>Barcodes</label>
            <div>{(p.barcodes || []).map((c) => <span className="bchip" key={c}>{c}<button className="mini" style={{ padding: "0 5px", border: "none" }} onClick={() => set("barcodes", p.barcodes.filter((x) => x !== c))}>✕</button></span>)}</div>
            <div style={{ display: "flex", gap: 8 }}>
              <input className="grow" placeholder="add a code" value={bc} onChange={(e) => setBc(e.target.value)} />
              <button className="mini" onClick={() => { if (bc.trim()) { set("barcodes", [...(p.barcodes || []), bc.trim()]); setBc(""); } }}>Add</button>
            </div>
          </div>
        </div>

        <div className="group">
          <div className="group-t">Units</div>
          <div className="frow">
            <div className="field"><label>Purchase unit</label><input value={p.purchase_unit} onChange={(e) => set("purchase_unit", e.target.value)} /></div>
            <div className="field"><label>Pack (per case)</label><input type="number" value={p.pack ?? ""} onChange={(e) => set("pack", num(e.target.value))} /></div>
          </div>
          <div className="frow">
            <div className="field"><label>Size</label><input type="number" value={p.size ?? ""} onChange={(e) => set("size", num(e.target.value))} /></div>
            <div className="field"><label>Size unit</label><input value={p.size_unit || ""} onChange={(e) => set("size_unit", e.target.value)} placeholder="LB, OZ, CT…" /></div>
          </div>
          <div className="frow">
            <div className="field"><label>Count unit</label><input value={p.count_unit} onChange={(e) => set("count_unit", e.target.value)} placeholder="bag, can, each…" /></div>
            <div className="field"><label>Count units / case</label><input type="number" value={p.count_per_case ?? ""} onChange={(e) => set("count_per_case", num(e.target.value))} /></div>
          </div>
          <div className="frow">
            <div className="field"><label>Use unit</label><input value={p.use_unit || ""} onChange={(e) => set("use_unit", e.target.value)} placeholder="oz, cup, each…" /></div>
            <div className="field"><label>Use / count unit</label><input type="number" value={p.use_per_count ?? ""} onChange={(e) => set("use_per_count", num(e.target.value))} /></div>
          </div>
        </div>

        <div className="group">
          <div className="group-t">Storage locations</div>
          {p.locations.map((l, i) => {
            const locUnits = (units || []).filter((u) => u.location_id === l.location_id);
            return (
            <div className="vrow" key={i}>
              <select value={l.location_id} onChange={(e) => { const id = Number(e.target.value); const nm = locations.find((x) => x.location_id === id)?.name; setP((s) => ({ ...s, locations: s.locations.map((x, j) => j === i ? { ...x, location_id: id, name: nm, unit_id: null } : x) })); }} style={{ flex: 2 }}>
                {locations.map((o) => <option key={o.location_id} value={o.location_id}>{o.name}</option>)}
              </select>
              <select value={l.unit_id ?? ""} disabled={!locUnits.length} onChange={(e) => { const uid = e.target.value ? Number(e.target.value) : null; setP((s) => ({ ...s, locations: s.locations.map((x, j) => j === i ? { ...x, unit_id: uid } : x) })); }} style={{ flex: 1 }} title="Shelf / bin">
                <option value="">{locUnits.length ? "— shelf —" : "no shelves"}</option>
                {locUnits.map((u) => <option key={u.storage_unit_id} value={u.storage_unit_id}>{u.code}</option>)}
              </select>
              <button className="mini" style={{ background: l.primary ? "#191B1F" : "#fff", color: l.primary ? "#fff" : "#191B1F" }} onClick={() => setP((s) => ({ ...s, locations: s.locations.map((x, j) => ({ ...x, primary: j === i })) }))}>★</button>
              <button className="mini mini-danger" onClick={() => setP((s) => ({ ...s, locations: s.locations.filter((_, j) => j !== i) }))}>✕</button>
            </div>
          );})}
          <button className="mini" onClick={addLoc}>+ Add location</button>
        </div>

        <div className="group">
          <div className="group-t">Vendors &amp; pricing</div>
          {p.vendors.map((v, i) => (
            <div className="vrow" key={i}>
              <select value={v.vendor_id} onChange={(e) => { const id = Number(e.target.value); const nm = vendors.find((x) => x.vendor_id === id)?.name; setP((s) => ({ ...s, vendors: s.vendors.map((x, j) => j === i ? { ...x, vendor_id: id, name: nm } : x) })); }} style={{ flex: 1 }}>
                {vendors.map((o) => <option key={o.vendor_id} value={o.vendor_id}>{o.name}</option>)}
              </select>
              <input type="number" step="0.01" placeholder="case $" value={v.price ?? ""} onChange={(e) => setP((s) => ({ ...s, vendors: s.vendors.map((x, j) => j === i ? { ...x, price: num(e.target.value) } : x) }))} style={{ width: 90 }} />
              <button className="mini" style={{ background: v.primary ? "#191B1F" : "#fff", color: v.primary ? "#fff" : "#191B1F" }} onClick={() => setP((s) => ({ ...s, vendors: s.vendors.map((x, j) => ({ ...x, primary: j === i })) }))}>★</button>
              <button className="mini mini-danger" onClick={() => setP((s) => ({ ...s, vendors: s.vendors.filter((_, j) => j !== i) }))}>✕</button>
            </div>
          ))}
          <button className="mini" onClick={addVendor}>+ Add vendor</button>
        </div>

        <div style={{ display: "flex", gap: 10, marginTop: 18, marginBottom: 30 }}>
          <button className="btn btn-primary" style={{ flex: 1 }} disabled={!p.name || busy} onClick={save}>{busy ? <><span className="spin" /> Saving…</> : "Save product"}</button>
          <button className="btn btn-ghost" onClick={onClose}>Cancel</button>
        </div>
      </div>
    </div>
  );
}

function Count({ products, locations, onhand, reload }) {
  const [locId, setLocId] = useState("");
  const [draft, setDraft] = useState({});
  const [note, setNote] = useState(0);
  const [q, setQ] = useState("");
  const [finding, setFinding] = useState(false);
  const [focusId, setFocusId] = useState(null);
  const setRow = (key, f, v) => setDraft((d) => ({ ...d, [key]: { ...(d[key] || {}), [f]: v } }));

  const loc = locations.find((l) => String(l.location_id) === String(locId));
  const unitFor = (p) => (p.locations || []).find((l) => l.location_id === loc?.location_id);
  let list = loc ? products.filter((p) => (p.locations || []).some((l) => l.location_id === loc.location_id)) : products;
  if (q) list = list.filter((p) => p.name.toLowerCase().includes(q.toLowerCase()) || (p.brand || "").toLowerCase().includes(q.toLowerCase()) || (p.barcodes || []).includes(q));

  const groups = {};
  for (const p of list) {
    let gname, gsort;
    if (loc) { const u = unitFor(p); gname = u?.unit_code || "Unassigned shelf"; gsort = u?.unit_sort ?? 9e9; }
    else { gname = (p.locations || []).find((l) => l.primary)?.name || (p.locations || [])[0]?.name || "Unassigned"; gsort = 0; }
    (groups[gname] = groups[gname] || { sort: gsort, items: [] }).items.push(p);
  }
  for (const g of Object.values(groups)) g.items.sort((a, b) => a.name.localeCompare(b.name));
  const orderedGroups = Object.entries(groups).sort((a, b) => (a[1].sort - b[1].sort) || a[0].localeCompare(b[0]));
  const entered = Object.entries(draft).filter(([k, e]) => (e.cases !== undefined && e.cases !== "") || (e.loose !== undefined && e.loose !== ""));

  async function save() {
    if (!loc) { alert("Pick a location first so each count is recorded to the right spot."); return; }
    const entries = entered.map(([k, e]) => {
      const pid = Number(k);
      const p = products.find((x) => x.product_id === pid);
      return { product_id: pid, location_id: loc.location_id, cases: e.cases, loose: e.loose, count_per_case: p.count_per_case };
    });
    await db.postCounts(entries);
    setNote(entries.length); setDraft({}); reload();
  }

  function onFound(p) {
    setFinding(false);
    const hasLoc = loc && (p.locations || []).some((l) => l.location_id === loc.location_id);
    if (!hasLoc) {
      const target = (p.locations || []).find((l) => l.primary) || (p.locations || [])[0];
      if (target) { setLocId(String(target.location_id)); setDraft({}); }
    }
    setQ(p.name); setFocusId(p.product_id);
  }

  return (
    <div>
      <div className="toolbar">
        <select value={locId} onChange={(e) => { setLocId(e.target.value); setDraft({}); setQ(""); setFocusId(null); }}>
          <option value="">Pick a location to count…</option>
          {locations.map((l) => <option key={l.location_id} value={l.location_id}>{l.name}</option>)}
        </select>
        <input className="grow" placeholder={loc ? "Search this location…" : "Search all items…"} value={q} onChange={(e) => setQ(e.target.value)} />
        <button className="mini" onClick={() => setFinding(true)}>📷 Find</button>
        <button className="btn btn-primary" disabled={!entered.length} onClick={save}>Save {entered.length || ""} count{entered.length === 1 ? "" : "s"}</button>
      </div>
      <p className="stat" style={{ margin: "0 2px 12px" }}>Items list in shelf order (A1, A2, …). Count cases and loose separately — the total is figured for you. Each location is a partial count; on-hand sums across locations.</p>
      {note > 0 && <div className="ok">Saved {note} count{note === 1 ? "" : "s"}. On-hand updated.</div>}
      {!loc && <div className="note" style={{ marginBottom: 14 }}>Pick a location above to count the items stored there — they'll be sorted by shelf.</div>}

      {orderedGroups.map(([g, grp]) => (
        <div className="vgroup" key={g}>
          <div className="vgroup-h"><span className="vname">{loc ? "▸ " + g : "📍 " + g}</span><span className="stat">{grp.items.length} items</span></div>
          {grp.items.map((p) => {
            const key = String(p.product_id);
            const e = draft[key] || {};
            const here = (loc ? onhand[p.product_id]?.byLoc?.[loc.name] : onhand[p.product_id]?.total) ?? 0;
            const counted = (e.cases !== undefined && e.cases !== "") || (e.loose !== undefined && e.loose !== "");
            const partial = (num(e.cases) || 0) * (p.count_per_case || 1) + (num(e.loose) || 0);
            const hot = focusId === p.product_id;
            return (
              <div className="crow" key={p.product_id} style={hot ? { background: "#FFF8E1", borderRadius: 8 } : undefined}>
                <div><b>{p.name}</b><div className="stat">1 case = {p.count_per_case} {p.count_unit} · here {here} · total {onhand[p.product_id]?.total ?? 0}</div></div>
                <label>Cases<input className="fig" type="number" min="0" value={e.cases ?? ""} onChange={(ev) => setRow(key, "cases", ev.target.value)} /></label>
                <label>Loose {p.count_unit}<input className="fig" type="number" min="0" value={e.loose ?? ""} onChange={(ev) => setRow(key, "loose", ev.target.value)} /></label>
                <div className="ctotal" style={{ color: counted ? "#191B1F" : "#B7BBC4" }}>= {counted ? partial : here}</div>
              </div>
            );
          })}
        </div>
      ))}
      {finding && <Finder products={products} onClose={() => setFinding(false)} onFound={onFound} />}
    </div>
  );
}

function Receive({ products, vendors, reload }) {
  const [rows, setRows] = useState(null);   // awaiting (purchased) rows, editable
  const [extras, setExtras] = useState([]); // scanned lines not on the purchased list
  const [busy, setBusy] = useState(false);
  const [msg, setMsg] = useState("");
  const [err, setErr] = useState("");
  const byId = Object.fromEntries(products.map((p) => [p.product_id, p]));

  async function load() {
    try {
      const r = await db.getReceivables();
      setRows(r.map((x) => ({ ...x, case_cost: x.unit_cost ?? "", scanned: false })));
    } catch (e) { setErr("Couldn't load purchased items: " + (e.message || e)); setRows([]); }
  }
  useEffect(() => { load(); }, []);
  if (rows === null) return <div className="empty">Loading…</div>;

  const setRow = (id, patch) => setRows((rs) => rs.map((r) => r.shopping_line_id === id ? { ...r, ...patch } : r));
  const setExtra = (i, patch) => setExtras((es) => es.map((e, j) => j === i ? { ...e, ...patch } : e));

  async function onFile(e) {
    const f = e.target.files?.[0]; if (!f) return;
    setBusy(true); setErr(""); setMsg("");
    try {
      const d = await db.scanReceipt(f, "receipt");
      const ven = vendors.find((v) => (d.vendor || "").toLowerCase().includes(v.name.toLowerCase()));
      const next = rows.map((r) => ({ ...r }));
      let matched = 0; const newExtras = [];
      for (const ln of (d.lines || [])) {
        const key = (ln.name || "").toLowerCase().slice(0, 6);
        const hit = key && next.find((r) => !r.scanned && r.product_name?.toLowerCase().includes(key));
        if (hit) { hit.qty = ln.qty ?? hit.qty; hit.case_cost = ln.unit_price ?? hit.case_cost; hit.scanned = true; matched++; }
        else {
          const m = products.find((p) => key && p.name.toLowerCase().includes(key));
          newExtras.push({ product_id: m?.product_id || "", qty: ln.qty || 1, case_cost: ln.unit_price ?? "", vendor_id: ven?.vendor_id || (vendors[0]?.vendor_id ?? ""), guess: ln.name });
        }
      }
      setRows(next);
      setExtras((es) => [...es, ...newExtras]);
      setMsg(`Receipt scanned — ${matched} matched to purchased item${matched === 1 ? "" : "s"}${newExtras.length ? `, ${newExtras.length} not on the list (below)` : ""}. Review the numbers, then Receive.`);
    } catch (e2) { setErr("Couldn't read that receipt. You can still receive items by hand."); }
    finally { setBusy(false); e.target.value = ""; }
  }

  async function receive(list) {
    const payload = list.map((r) => ({
      shopping_line_id: r.shopping_line_id, product_id: Number(r.product_id),
      vendor_id: r.vendor_id ? Number(r.vendor_id) : null,
      qty: num(r.qty), unit_cost: (r.case_cost !== "" && r.case_cost != null) ? num(r.case_cost) : null,
      count_per_case: r.count_per_case ?? byId[r.product_id]?.count_per_case ?? 1,
    })).filter((r) => r.product_id && num(r.qty) > 0);
    if (!payload.length) return;
    setBusy(true);
    try { await db.receiveRows(payload); setMsg(`Received ${payload.length} item${payload.length === 1 ? "" : "s"}. Prices updated.`); await load(); reload(); }
    catch (e) { setErr("Receive failed: " + (e.message || e)); }
    finally { setBusy(false); }
  }
  async function receiveExtras(idxs) {
    await receive(idxs.map((i) => extras[i]));
    setExtras((es) => es.filter((_, i) => !idxs.includes(i)));
  }

  const groups = {};
  for (const r of rows) { const k = r.vendor_id ?? "none"; (groups[k] ||= []).push(r); }
  const order = Object.keys(groups);

  return (
    <div>
      <div className="toolbar">
        <label className="btn btn-primary">{busy ? <><span className="spin" /> Working…</> : "📷 Scan a receipt"}<input type="file" accept="image/*" capture="environment" style={{ display: "none" }} onChange={onFile} disabled={busy} /></label>
        {rows.length > 0 && <button className="btn btn-ghost" onClick={() => receive(rows)}>Receive everything</button>}
      </div>
      {err && <div className="err">{err}</div>}
      {msg && <div className="ok">{msg}</div>}

      {rows.length === 0 && extras.length === 0 && (
        <div className="empty">Nothing waiting to be received.<br />Mark items <b>Purchased</b> on the Shopping tab and they'll appear here — or scan a receipt.</div>
      )}

      {order.map((k) => {
        const g = groups[k]; const vn = g[0].vendor_name || "No vendor";
        return (
          <div className="vgroup" key={k}>
            <div className="vgroup-h">
              <span className="vname">{vn}</span>
              <span style={{ display: "flex", gap: 8, alignItems: "center" }}>
                <span className="stat">{g.length} awaiting</span>
                <button className="mini" onClick={() => receive(g)}>Receive all</button>
              </span>
            </div>
            <table className="tbl" style={{ border: "none" }}>
              <thead><tr><th>Item</th><th>Qty (cases)</th><th>Case $</th><th></th></tr></thead>
              <tbody>{g.map((r) => (
                <tr key={r.shopping_line_id}>
                  <td>{r.product_name}{r.scanned && <span className="bchip" style={{ marginLeft: 6, background: "#E6F4F0", borderColor: "#0E7C6B" }}>from receipt</span>}<div className="stat">1 case = {r.count_per_case} {r.count_unit}</div></td>
                  <td><input className="fig" style={{ width: 70 }} type="number" min="0" value={r.qty} onChange={(e) => setRow(r.shopping_line_id, { qty: e.target.value })} /></td>
                  <td><input className="fig" style={{ width: 80 }} type="number" step="0.01" value={r.case_cost} onChange={(e) => setRow(r.shopping_line_id, { case_cost: e.target.value })} /></td>
                  <td><button className="mini" onClick={() => receive([r])}>Receive</button></td>
                </tr>
              ))}</tbody>
            </table>
          </div>
        );
      })}

      {extras.length > 0 && (
        <div className="vgroup">
          <div className="vgroup-h"><span className="vname">Not on the purchased list</span>
            <button className="mini" onClick={() => receiveExtras(extras.map((_, i) => i))}>Receive all</button></div>
          <table className="tbl" style={{ border: "none" }}>
            <thead><tr><th>Item (from receipt)</th><th>Qty</th><th>Case $</th><th>Vendor</th><th></th></tr></thead>
            <tbody>{extras.map((r, i) => (
              <tr key={i}>
                <td style={{ minWidth: 150 }}><select value={r.product_id} onChange={(e) => setExtra(i, { product_id: e.target.value })} style={{ width: "100%" }}>
                  <option value="">{r.guess ? `? ${r.guess}` : "— pick —"}</option>
                  {products.slice().sort((a, b) => a.name.localeCompare(b.name)).map((p) => <option key={p.product_id} value={p.product_id}>{p.name}</option>)}
                </select></td>
                <td><input className="fig" style={{ width: 60 }} type="number" value={r.qty} onChange={(e) => setExtra(i, { qty: e.target.value })} /></td>
                <td><input className="fig" style={{ width: 70 }} type="number" step="0.01" value={r.case_cost} onChange={(e) => setExtra(i, { case_cost: e.target.value })} /></td>
                <td><select value={r.vendor_id} onChange={(e) => setExtra(i, { vendor_id: e.target.value })}>{vendors.map((v) => <option key={v.vendor_id} value={v.vendor_id}>{v.name}</option>)}</select></td>
                <td><button className="mini" onClick={() => receiveExtras([i])}>Receive</button></td>
              </tr>
            ))}</tbody>
          </table>
        </div>
      )}
    </div>
  );
}

function computeItemStats(products, counts, receipts) {
  const wkk = (d) => weekStart(d.slice(0, 10)).toISOString().slice(0, 10);
  const byItem = {};
  for (const c of counts) {
    const w = wkk(c.counted_at);
    const it = (byItem[c.product_id] ||= {}); const wo = (it[w] ||= {});
    const t = new Date(c.counted_at).getTime();
    if (!wo[c.location_id] || t >= wo[c.location_id].t) wo[c.location_id] = { qty: Number(c.qty), t };
  }
  const recByItem = {};
  for (const r of receipts) for (const l of (r.receipt_line || [])) {
    const w = wkk(r.received_date); (recByItem[l.product_id] ||= {});
    recByItem[l.product_id][w] = (recByItem[l.product_id][w] || 0) + (Number(l.qty_count_units) || Number(l.purchase_qty) || 0);
  }
  const stats = {};
  for (const p of products) {
    const cpc = p.count_per_case || 1;
    const it = byItem[p.product_id];
    if (!it) { stats[p.product_id] = { onhandUnits: 0, usedUnits: 0, suggestCases: 0 }; continue; }
    const weeks = Object.keys(it).sort();
    const total = (w) => Object.values(it[w]).reduce((a, x) => a + x.qty, 0);
    const usages = [];
    for (let i = 1; i < weeks.length; i++) {
      const recd = (recByItem[p.product_id] || {})[weeks[i]] || 0;
      let u = total(weeks[i - 1]) + recd - total(weeks[i]); if (u < 0) u = 0; usages.push(u);
    }
    const last = usages.length ? usages[usages.length - 1] : 0;
    const avg = usages.length ? usages.reduce((a, x) => a + x, 0) / usages.length : 0;
    const forecast = 0.6 * last + 0.4 * avg;
    const onhandUnits = total(weeks[weeks.length - 1]);
    const target = Math.max(Number(p.par_level) || 0, forecast / cpc);
    const suggestCases = Math.max(0, Math.ceil(target - onhandUnits / cpc - 1e-9));
    stats[p.product_id] = { onhandUnits, usedUnits: last, suggestCases };
  }
  return stats;
}

function computeSuggestions(products, counts, receipts) {
  const wkk = (d) => weekStart(d.slice(0, 10)).toISOString().slice(0, 10);
  const byItem = {};
  for (const c of counts) {
    const w = wkk(c.counted_at);
    const it = (byItem[c.product_id] ||= {});
    const wo = (it[w] ||= {});
    const t = new Date(c.counted_at).getTime();
    if (!wo[c.location_id] || t >= wo[c.location_id].t) wo[c.location_id] = { qty: Number(c.qty), t };
  }
  const recByItem = {};
  for (const r of receipts) for (const l of (r.receipt_line || [])) {
    const w = wkk(r.received_date);
    (recByItem[l.product_id] ||= {});
    recByItem[l.product_id][w] = (recByItem[l.product_id][w] || 0) + (Number(l.qty_count_units) || (Number(l.purchase_qty) || 0));
  }
  const out = [];
  for (const p of products) {
    const it = byItem[p.product_id]; if (!it) continue;        // need at least one count to know on-hand
    const weeks = Object.keys(it).sort();
    const total = (w) => Object.values(it[w]).reduce((a, x) => a + x.qty, 0);
    const usages = [];
    for (let i = 1; i < weeks.length; i++) {
      const recd = (recByItem[p.product_id] || {})[weeks[i]] || 0;
      let u = total(weeks[i - 1]) + recd - total(weeks[i]); if (u < 0) u = 0;
      usages.push(u);
    }
    const last = usages.length ? usages[usages.length - 1] : 0;
    const avg = usages.length ? usages.reduce((a, x) => a + x, 0) / usages.length : 0;
    const forecast = 0.6 * last + 0.4 * avg;                   // count units used per week
    const cpc = p.count_per_case || 1;
    const onhandCases = total(weeks[weeks.length - 1]) / cpc;
    const usageCases = forecast / cpc;
    const parCases = Number(p.par_level) || 0;
    const target = Math.max(parCases, usageCases);             // build up to par, or a week's usage if higher
    const cases = Math.ceil(target - onhandCases - 1e-9);
    if (cases >= 1) out.push({ product: p, cases, target: +target.toFixed(1), usageCases: +usageCases.toFixed(1), onhandCases: +onhandCases.toFixed(1) });
  }
  out.sort((a, b) => b.cases - a.cases);
  return out;
}

function Shopping({ products, vendors, onhand }) {
  const [listId, setListId] = useState(null);
  const [lines, setLines] = useState(null);
  const [counts, setCounts] = useState([]);
  const [receipts, setReceipts] = useState([]);
  const [note, setNote] = useState("");
  const byId = Object.fromEntries(products.map((p) => [p.product_id, p]));
  const vName = Object.fromEntries(vendors.map((v) => [v.vendor_id, v.name]));

  async function load() {
    try { const d = await db.getShopping(); setListId(d.id); setLines(d.lines); }
    catch { setLines([]); }
  }
  useEffect(() => { load(); }, []);
  useEffect(() => {
    db.getCounts(120).then(setCounts).catch(() => {});
    db.getReceipts(120).then(setReceipts).catch(() => {});
  }, []);
  if (lines === null) return <div className="empty">Loading…</div>;
  const suggestions = computeSuggestions(products, counts, receipts);
  const stats = computeItemStats(products, counts, receipts);
  const r1 = (n) => Math.round(n * 10) / 10;

  async function add(p) {
    const v = p.vendors.find((x) => x.primary) || p.vendors[0];
    await db.addShoppingLine(listId, { product_id: p.product_id, vendor_id: v?.vendor_id ?? null, qty: 1, unit_cost: v?.price ?? null });
    load();
  }
  async function setQty(line, qty) {
    setLines((ls) => ls.map((l) => l.shopping_line_id === line.shopping_line_id ? { ...l, qty } : l));
    await db.updateShoppingLine(line.shopping_line_id, { qty: num(qty) || 0 });
  }
  async function setVendor(line, vendor_id) {
    const price = byId[line.product_id]?.vendors.find((v) => v.vendor_id === vendor_id)?.price ?? null;
    await db.updateShoppingLine(line.shopping_line_id, { vendor_id, unit_cost: price });
    load();
  }
  async function purchase(line, on) { await db.updateShoppingLine(line.shopping_line_id, { status: on ? "purchased" : "open" }); load(); }
  async function purchaseVendor(vid, on) { await db.setVendorStatus(listId, vid, on ? "purchased" : "open", on ? "open" : "purchased"); load(); }
  async function remove(line) { await db.removeShoppingLine(line.shopping_line_id); load(); }
  async function buildFromUsage() {
    const have = new Set(lines.map((l) => l.product_id));
    let added = 0;
    for (const s of suggestions) {
      if (have.has(s.product.product_id)) continue;
      const v = s.product.vendors.find((x) => x.primary) || s.product.vendors[0];
      await db.addShoppingLine(listId, { product_id: s.product.product_id, vendor_id: v?.vendor_id ?? null, qty: s.cases, unit_cost: v?.price ?? null });
      added++;
    }
    setNote(added ? `Added ${added} item${added === 1 ? "" : "s"} from last week's usage. Review the quantities, then mark Purchased.` : "Everything suggested is already on your list.");
    load();
  }

  const groups = {};
  for (const l of lines) { const k = l.vendor_id ?? "none"; (groups[k] ||= []).push(l); }
  const order = Object.keys(groups).sort((a, b) => (vName[a] || "zz").localeCompare(vName[b] || "zz"));

  return (
    <div>
      <div className="toolbar">
        <Picker products={products} exclude={lines.map((l) => l.product_id)} onPick={add} />
        <button className="btn btn-ghost" disabled={!suggestions.length} onClick={buildFromUsage}>🧮 Build from usage{suggestions.length ? ` (${suggestions.length})` : ""}</button>
      </div>
      {note && <div className="ok">{note}</div>}
      <div className="note" style={{ marginBottom: 14 }}>Add items by hand, or tap <b>Build from usage</b> to fill the order up to each item's <b>par</b> (or a week's usage if that's higher), minus what's on hand, rounded up to whole cases. Then mark items <b>Purchased</b> — one at a time or a whole vendor at once — and they move to the <b>Receive</b> tab.</div>
      {lines.length === 0 ? <div className="empty">Add items to build your order.</div> : order.map((k) => {
        const g = groups[k];
        const openCount = g.filter((l) => l.status === "open").length;
        return (
          <div className="vgroup" key={k}>
            <div className="vgroup-h">
              <span className="vname">{vName[k] || "No vendor"}</span>
              <span style={{ display: "flex", gap: 8, alignItems: "center" }}>
                <span className="stat">{g.length} items</span>
                {openCount > 0
                  ? <button className="mini" onClick={() => purchaseVendor(k === "none" ? null : Number(k), true)}>Mark all purchased</button>
                  : <button className="mini" onClick={() => purchaseVendor(k === "none" ? null : Number(k), false)}>Undo all</button>}
              </span>
            </div>
            {g.map((l) => {
              const p = byId[l.product_id]; if (!p) return null;
              const done = l.status === "purchased";
              return (
                <div className="crow" style={{ gridTemplateColumns: "1fr 130px 70px 130px", opacity: done ? 0.55 : 1 }} key={l.shopping_line_id}>
                  <div><b>{p.name}</b><div className="stat">on hand <b style={{ color: "#191B1F" }}>{r1(onhand?.[p.product_id]?.total ?? 0)}</b> {p.count_unit} · used last wk <b style={{ color: "#191B1F" }}>{r1(stats[p.product_id]?.usedUnits ?? 0)}</b> · suggest <b style={{ color: "#191B1F" }}>{stats[p.product_id]?.suggestCases ?? 0}</b> {stats[p.product_id]?.suggestCases === 1 ? "case" : "cases"}</div></div>
                  <select value={l.vendor_id ?? ""} onChange={(e) => setVendor(l, Number(e.target.value))} disabled={done}>
                    {(p.vendors || []).map((v) => <option key={v.vendor_id} value={v.vendor_id}>{v.name}{v.price != null ? ` (${money(v.price)})` : ""}</option>)}
                  </select>
                  <input className="fig" type="number" min="0" value={l.qty} onChange={(e) => setQty(l, e.target.value)} disabled={done} />
                  <div style={{ display: "flex", gap: 6, justifyContent: "flex-end" }}>
                    {done
                      ? <button className="mini" style={{ background: "#E6F4F0", borderColor: "#0E7C6B", color: "#0a5c50" }} onClick={() => purchase(l, false)}>✓ Purchased</button>
                      : <>
                          <button className="mini" style={{ borderColor: "#0E7C6B", color: "#0a5c50" }} onClick={() => purchase(l, true)}>Purchased</button>
                          <button className="mini mini-danger" onClick={() => remove(l)}>✕</button>
                        </>}
                  </div>
                </div>
              );
            })}
          </div>
        );
      })}
    </div>
  );
}

function Finder({ products, onClose, onFound }) {
  const [q, setQ] = useState("");
  const [status, setStatus] = useState("");
  const [photoHits, setPhotoHits] = useState(null);
  const [linkCode, setLinkCode] = useState(null);
  const [camOn, setCamOn] = useState(false);
  const inputRef = useRef(null);
  const videoRef = useRef(null);
  const streamRef = useRef(null);
  const rafRef = useRef(null);
  const stoppedRef = useRef(false);
  const byId = Object.fromEntries(products.map((p) => [p.product_id, p]));
  const camSupported = typeof window !== "undefined" && "BarcodeDetector" in window;

  useEffect(() => { inputRef.current?.focus(); }, []);

  const term = q.trim().toLowerCase();
  const nameHits = term
    ? products.filter((p) => p.name.toLowerCase().includes(term) || (p.brand || "").toLowerCase().includes(term)
        || (p.barcodes || []).some((b) => String(b).includes(q.trim())) || String(p.supc || "").includes(q.trim())).slice(0, 12)
    : [];

  function stopCam() {
    stoppedRef.current = true;
    if (rafRef.current) cancelAnimationFrame(rafRef.current);
    const s = streamRef.current; if (s) { try { s.getTracks().forEach((t) => t.stop()); } catch {} }
    streamRef.current = null; setCamOn(false);
  }
  function finish(p) { stopCam(); onFound(p); }

  async function lookupCode(raw) {
    const code = String(raw ?? q).trim(); if (!code) return;
    setStatus(`Looking up ${code}…`); setPhotoHits(null);
    try {
      const hit = await db.lookupBarcode(code);
      if (hit) { finish(byId[hit.product_id] || hit); return; }
      setLinkCode(code); setStatus(`No item is linked to “${code}.” Pick the product below to link it.`);
    } catch (e) { setStatus("Lookup failed: " + (e.message || e)); }
  }

  function onEnter() {
    const code = q.trim(); if (!code) return;
    if (/^\d{6,}$/.test(code)) lookupCode(code);     // looks like a barcode / SUPC
    else if (nameHits[0]) finish(nameHits[0]);        // otherwise jump to best name match
    else lookupCode(code);
  }

  async function startCam() {
    if (!camSupported) { setStatus("This browser can't scan with the camera. Type the barcode above, or use a handheld scanner."); return; }
    setLinkCode(null); setPhotoHits(null); setCamOn(true); stoppedRef.current = false;
    setStatus("Starting camera…");
    let detector;
    try { detector = new window.BarcodeDetector({ formats: ["ean_13", "ean_8", "upc_a", "upc_e", "code_128", "code_39", "itf", "codabar"] }); }
    catch { detector = new window.BarcodeDetector(); }
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: "environment" } });
      if (stoppedRef.current) { stream.getTracks().forEach((t) => t.stop()); return; }
      streamRef.current = stream; videoRef.current.srcObject = stream; await videoRef.current.play();
      setStatus("Point the camera at a barcode…");
      const tick = async () => {
        if (stoppedRef.current) return;
        try { const codes = await detector.detect(videoRef.current); if (codes && codes[0]) { setQ(codes[0].rawValue); lookupCode(codes[0].rawValue); return; } } catch {}
        rafRef.current = requestAnimationFrame(tick);
      };
      rafRef.current = requestAnimationFrame(tick);
    } catch { setCamOn(false); setStatus("Couldn't open the camera. Check permissions, or type the barcode above."); }
  }

  async function onPhoto(e) {
    const f = e.target.files?.[0]; if (!f) return;
    setStatus("Identifying…"); setPhotoHits(null); setLinkCode(null);
    try {
      const d = await db.scanReceipt(f, "product");
      if (d?.barcode && /^\d{6,}$/.test(String(d.barcode))) { await lookupCode(String(d.barcode)); e.target.value = ""; return; }
      const guess = String(d?.name || d?.brand || "").toLowerCase();
      const key = guess.slice(0, 5);
      const found = key ? products.filter((p) => p.name.toLowerCase().includes(key) || guess.includes(p.name.toLowerCase().slice(0, 5))).slice(0, 8) : [];
      setPhotoHits(found);
      setStatus(found.length ? `Closest matches${d?.name ? ` for “${d.name}”` : ""} — tap the right one:` : "Couldn't match that photo. Type the name or barcode above.");
    } catch { setStatus("Photo identify isn't set up right now. Type the name or barcode above."); }
    e.target.value = "";
  }

  async function pick(p) {
    if (linkCode) { try { await db.linkBarcode(p.product_id, linkCode); } catch {} }
    finish(p);
  }

  const list = linkCode ? nameHits : (photoHits != null ? photoHits : nameHits);

  return (
    <div className="overlay" onClick={() => { stopCam(); onClose(); }}>
      <div className="sheet" onClick={(e) => e.stopPropagation()} style={{ width: "min(480px,100%)" }}>
        <h2>Find an item</h2>
        <div className="stat" style={{ marginBottom: 8 }}>Type or scan a barcode/SUPC, or search by name. Press Enter to jump to the best match.</div>
        <input ref={inputRef} style={{ width: "100%", marginBottom: 8 }} placeholder="Scan / type barcode, or search by name…"
          value={q} onChange={(e) => { setQ(e.target.value); setLinkCode(null); setPhotoHits(null); }}
          onKeyDown={(e) => { if (e.key === "Enter") onEnter(); }} />
        <div style={{ display: "flex", gap: 8, marginBottom: 10, flexWrap: "wrap" }}>
          <button className="mini" onClick={onEnter}>Find</button>
          {camSupported && <button className="mini" onClick={() => (camOn ? stopCam() : startCam())}>{camOn ? "Stop camera" : "📷 Scan with camera"}</button>}
          <label className="mini" style={{ cursor: "pointer" }}>🖼 Photo of product<input type="file" accept="image/*" capture="environment" style={{ display: "none" }} onChange={onPhoto} /></label>
        </div>

        {camOn && <video ref={videoRef} style={{ width: "100%", borderRadius: 10, background: "#000", aspectRatio: "4 / 3", objectFit: "cover", marginBottom: 8 }} muted playsInline />}
        {status && <p className="stat" style={{ marginTop: 0 }}>{status}</p>}
        {linkCode && <div className="group-t">Link “{linkCode}” to:</div>}

        <div style={{ maxHeight: 260, overflowY: "auto" }}>
          {list.map((p) => (
            <button key={p.product_id} className="mini" style={{ display: "block", width: "100%", textAlign: "left", marginBottom: 4 }} onClick={() => pick(p)}>
              {p.name}<span className="stat"> · {(p.locations || []).map((l) => l.name + (l.unit_code ? ` ${l.unit_code}` : "")).join(", ") || "no location"}</span>
            </button>
          ))}
          {list.length === 0 && term && <div className="stat">No matches — check spelling, or scan/type the barcode.</div>}
        </div>

        <button className="btn btn-ghost" style={{ marginTop: 14 }} onClick={() => { stopCam(); onClose(); }}>Close</button>
      </div>
    </div>
  );
}

function Picker({ products, exclude, onPick }) {
  const [q, setQ] = useState("");
  const hits = q ? products.filter((p) => p.name.toLowerCase().includes(q.toLowerCase()) && !(exclude || []).includes(p.product_id)).slice(0, 8) : [];
  return (
    <div className="grow" style={{ position: "relative" }}>
      <input style={{ width: "100%" }} placeholder="Add an item…" value={q} onChange={(e) => setQ(e.target.value)} />
      {hits.length > 0 && (
        <div style={{ position: "absolute", zIndex: 6, top: "100%", left: 0, right: 0, background: "#fff", border: "1.5px solid #E6E1D6", borderRadius: 8, marginTop: 4, maxHeight: 220, overflowY: "auto" }}>
          {hits.map((p) => <button key={p.product_id} className="mini" style={{ display: "block", width: "100%", textAlign: "left", border: "none", borderRadius: 0 }} onClick={() => { onPick(p); setQ(""); }}>{p.name}</button>)}
        </div>
      )}
    </div>
  );
}

const fmtUSD = (n) => (n == null || isNaN(n)) ? "$0" : "$" + Number(n).toLocaleString(undefined, { maximumFractionDigits: 0 });
function costPerCount(p) {
  const v = (p.vendors || []).find((x) => x.primary) || (p.vendors || [])[0];
  return v?.price != null ? v.price / (p.count_per_case || 1) : null;
}
function lineCost(l) { return (Number(l.purchase_qty) || 0) * (Number(l.unit_cost) || 0); }

function weekStart(dateStr) {
  const dt = new Date(dateStr + "T00:00:00");
  const off = (dt.getDay() + 6) % 7; // Monday = start of week
  dt.setDate(dt.getDate() - off); dt.setHours(0, 0, 0, 0);
  return dt;
}
function computeWeekly(recs, n = 8) {
  const cur = weekStart(new Date().toISOString().slice(0, 10));
  const buckets = [];
  for (let i = n - 1; i >= 0; i--) {
    const d = new Date(cur); d.setDate(d.getDate() - i * 7);
    buckets.push({ key: d.toISOString().slice(0, 10), ts: d.getTime(), amt: 0 });
  }
  const idx = Object.fromEntries(buckets.map((b, i) => [b.key, i]));
  for (const r of recs) {
    const k = weekStart(r.received_date).toISOString().slice(0, 10);
    if (k in idx) buckets[idx[k]].amt += (r.receipt_line || []).reduce((a, l) => a + lineCost(l), 0);
  }
  return buckets.map((b, i) => ({
    ...b, current: i === buckets.length - 1,
    label: new Date(b.ts).toLocaleDateString(undefined, { month: "short", day: "numeric" }),
  }));
}

function computeUsage(products, counts, receipts) {
  const byProd = {};
  for (const c of counts) {
    const d = c.counted_at.slice(0, 10);
    (byProd[c.product_id] ||= []).push({ d, loc: c.location_id, qty: Number(c.qty), t: new Date(c.counted_at).getTime() });
  }
  const recByProd = {};
  for (const r of receipts) for (const l of (r.receipt_line || [])) {
    (recByProd[l.product_id] ||= []).push({ d: r.received_date, units: Number(l.qty_count_units) || (Number(l.purchase_qty) || 0) });
  }
  const rows = []; let totalCost = 0; let interval = null;
  for (const p of products) {
    const evs = byProd[p.product_id]; if (!evs) continue;
    const days = [...new Set(evs.map((e) => e.d))].sort();
    if (days.length < 2) continue;
    const d1 = days[days.length - 2], d2 = days[days.length - 1];
    const ohAt = (day) => {
      const latest = {};
      for (const e of evs) if (e.d <= day && (!latest[e.loc] || e.t >= latest[e.loc].t)) latest[e.loc] = e;
      return Object.values(latest).reduce((a, e) => a + e.qty, 0);
    };
    const recd = (recByProd[p.product_id] || []).filter((x) => x.d > d1 && x.d <= d2).reduce((a, x) => a + x.units, 0);
    let used = ohAt(d1) + recd - ohAt(d2); if (used < 0) used = 0;
    const cpc = costPerCount(p); const cost = cpc != null ? used * cpc : null;
    if (cost != null) totalCost += cost;
    rows.push({ id: p.product_id, name: p.name, used, unit: p.count_unit, cost });
    interval = interval || [d1, d2];
  }
  rows.sort((a, b) => (b.cost || 0) - (a.cost || 0));
  return { rows, totalCost, interval };
}

function Dashboard({ products, onhand, vendors, reload }) {
  const [receipts, setReceipts] = useState(null);
  const [counts, setCounts] = useState([]);
  const [ship, setShip] = useState({ open: 0, purchased: 0 });
  const [open, setOpen] = useState("review");
  const [spendDays, setSpendDays] = useState(7);
  const [detail, setDetail] = useState(null);   // product whose count history is open

  useEffect(() => {
    db.getReceipts(120).then(setReceipts).catch(() => setReceipts([]));
    db.getCounts(120).then(setCounts).catch(() => setCounts([]));
    db.shoppingCounts().then(setShip).catch(() => {});
  }, []);

  const vName = Object.fromEntries(vendors.map((v) => [v.vendor_id, v.name]));
  const recs = receipts || [];

  let invValue = 0; const valByCat = {};
  for (const p of products) {
    const cpc = costPerCount(p); const oh = onhand[p.product_id]?.total || 0;
    if (cpc != null && oh) { const val = cpc * oh; invValue += val; const c = p.category || "Uncategorized"; valByCat[c] = (valByCat[c] || 0) + val; }
  }

  const sinceSpend = Date.now() - spendDays * 864e5;
  const spendByVendor = {};
  for (const r of recs) {
    if (new Date(r.received_date).getTime() < sinceSpend) continue;
    const c = (r.receipt_line || []).reduce((a, l) => a + lineCost(l), 0);
    spendByVendor[r.vendor_id ?? "none"] = (spendByVendor[r.vendor_id ?? "none"] || 0) + c;
  }
  const since30 = Date.now() - 30 * 864e5;
  const recs30 = recs.filter((r) => new Date(r.received_date).getTime() >= since30);
  const spend30 = recs30.reduce((a, r) => a + (r.receipt_line || []).reduce((s, l) => s + lineCost(l), 0), 0);
  const since7 = Date.now() - 7 * 864e5;
  const recs7 = recs.filter((r) => new Date(r.received_date).getTime() >= since7);
  const spend7 = recs7.reduce((a, r) => a + (r.receipt_line || []).reduce((s, l) => s + lineCost(l), 0), 0);
  const weekly = computeWeekly(recs, 8);

  const everyday = products.filter((p) => p.menu === "everyday").length;
  const events = products.filter((p) => p.menu === "events").length;
  const usage = computeUsage(products, counts, recs);

  const tile = (k, v, sub, dark) => <div className={"tile" + (dark ? " dark" : "")}><div className="k">{k}</div><div className="v fig">{v}</div>{sub && <div className="sub">{sub}</div>}</div>;
  const reports = [
    ["review", "Weekly review (last → received → this → used)", <WeeklyReviewReport counts={counts} receipts={recs} products={products} onOpen={setDetail} />],
    ["onhand", "On hand — by location", <OnHandReport products={products} onhand={onhand} />],
    ["value", "Inventory value — by category", <ValueReport valByCat={valByCat} total={invValue} />],
    ["spend", "Purchasing — by vendor", <SpendReport recs={recs} vName={vName} days={spendDays} setDays={setSpendDays} />],
    ["foodcost", "Weekly food cost & usage", <UsageReport usage={usage} />],
    ["counts", "Counts by week", <CountsByWeekReport counts={counts} products={products} onOpen={setDetail} />],
    ["complete", "Catalog completeness", <CompletenessReport products={products} />],
    ["menu", "Everyday vs Events", <MenuReport products={products} />],
  ];

  if (receipts === null) return <div className="empty">Loading…</div>;
  return (
    <div>
      <div className="tiles">
        {tile("Items tracked", products.length, `${everyday} everyday · ${events} events`)}
        {tile("Inventory value", fmtUSD(invValue), invValue ? "current count × cost" : "count items to populate", true)}
        {tile("Purchases · this week", fmtUSD(spend7), `${recs7.length} receipt${recs7.length === 1 ? "" : "s"} · 30d ${fmtUSD(spend30)}`)}
        {tile("To buy / awaiting", `${ship.open} / ${ship.purchased}`, "open · purchased")}
      </div>

      <div className="secthead">Spend by week · last 8 weeks</div>
      {weekly.every((w) => w.amt === 0)
        ? <div className="note">No receipts logged yet. Each week's spend builds here as you receive orders.</div>
        : <div style={{ background: "#fff", border: "1.5px solid #E6E1D6", borderRadius: 11, padding: 14 }}>
            {weekly.map((w) => {
              const max = Math.max(1, ...weekly.map((x) => x.amt));
              return <div key={w.key} style={{ marginBottom: 9 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginBottom: 3 }}>
                  <span>Week of {w.label}{w.current ? <b> · this week</b> : ""}</span><span className="fig">{money(w.amt)}</span>
                </div>
                <div className="bar"><span style={{ width: (w.amt / max * 100) + "%", background: w.current ? "#191B1F" : "#E0392B" }} /></div>
              </div>;
            })}
          </div>}

      <div className="secthead">Spend by vendor · last {spendDays} days</div>
      {Object.keys(spendByVendor).length === 0
        ? <div className="note">No receipts in this window yet. As you receive orders, spend shows up here.</div>
        : <div style={{ background: "#fff", border: "1.5px solid #E6E1D6", borderRadius: 11, padding: 14 }}>
            {Object.entries(spendByVendor).sort((a, b) => b[1] - a[1]).map(([vid, amt]) => {
              const max = Math.max(...Object.values(spendByVendor));
              return <div key={vid} style={{ marginBottom: 10 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginBottom: 3 }}><b>{vName[vid] || "No vendor"}</b><span className="fig">{money(amt)}</span></div>
                <div className="bar"><span style={{ width: (max ? amt / max * 100 : 0) + "%" }} /></div>
              </div>;
            })}
          </div>}

      <div className="secthead">Reports</div>
      {reports.map(([key, label, body]) => (
        <div className="panel" key={key}>
          <div className="panel-h" onClick={() => setOpen(open === key ? "" : key)}>
            <span>{label}</span><span style={{ color: "#71757E" }}>{open === key ? "▾" : "▸"}</span>
          </div>
          {open === key && <div className="panel-b">{body}</div>}
        </div>
      ))}
      {detail && <ItemHistory product={detail} onClose={() => setDetail(null)} onChanged={() => { db.getCounts(120).then(setCounts).catch(() => {}); reload && reload(); }} />}
    </div>
  );
}

function OnHandReport({ products, onhand }) {
  const rows = products.slice().sort((a, b) => a.name.localeCompare(b.name));
  return (
    <table className="tbl" style={{ border: "none" }}><thead><tr><th>Item</th><th>In stock</th><th>Where</th></tr></thead>
      <tbody>{rows.map((p) => {
        const oh = onhand[p.product_id];
        const where = oh ? Object.entries(oh.byLoc).filter(([, q]) => q > 0) : [];
        return (<tr key={p.product_id}>
          <td>{p.name}<div className="stat">{p.count_unit}</div></td>
          <td className="fig" style={{ fontWeight: 700 }}>{oh?.total ?? 0}</td>
          <td>{where.length ? where.map(([n, q]) => <span className="bchip" key={n}>{n}: <b className="fig" style={{ marginLeft: 4 }}>{q}</b></span>) : <span className="stat">not counted yet</span>}</td>
        </tr>);
      })}</tbody></table>
  );
}

function ValueReport({ valByCat, total }) {
  const rows = Object.entries(valByCat).sort((a, b) => b[1] - a[1]);
  if (!rows.length) return <div className="note">Inventory value appears once items have been counted — it's on-hand count × your cost per count unit.</div>;
  const max = Math.max(...rows.map((r) => r[1]));
  return (<div>
    <div style={{ fontFamily: "'Barlow Condensed',sans-serif", fontWeight: 700, fontSize: 22, marginBottom: 10 }}>{money(total)} <span style={{ fontSize: 13, color: "#71757E" }}>on hand at cost</span></div>
    {rows.map(([cat, amt]) => <div key={cat} style={{ marginBottom: 9 }}>
      <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginBottom: 3 }}><span>{cat}</span><span className="fig">{money(amt)}</span></div>
      <div className="bar"><span style={{ width: amt / max * 100 + "%" }} /></div>
    </div>)}
  </div>);
}

function SpendReport({ recs, vName, days, setDays }) {
  const since = Date.now() - days * 864e5;
  const inWin = recs.filter((r) => new Date(r.received_date).getTime() >= since);
  const byV = {}; let total = 0;
  for (const r of inWin) { const c = (r.receipt_line || []).reduce((a, l) => a + lineCost(l), 0); total += c; byV[r.vendor_id ?? "none"] = (byV[r.vendor_id ?? "none"] || 0) + c; }
  return (<div>
    <div style={{ display: "flex", gap: 8, marginBottom: 12 }}>
      {[7, 30, 90].map((d) => <button key={d} className="mini" style={{ background: days === d ? "#191B1F" : "#fff", color: days === d ? "#fff" : "#191B1F" }} onClick={() => setDays(d)}>{d} days</button>)}
    </div>
    {inWin.length === 0 ? <div className="note">No receipts in the last {days} days yet.</div> : (<div>
      <div style={{ fontFamily: "'Barlow Condensed',sans-serif", fontWeight: 700, fontSize: 22, marginBottom: 10 }}>{money(total)} <span style={{ fontSize: 13, color: "#71757E" }}>spent · {inWin.length} receipt{inWin.length === 1 ? "" : "s"}</span></div>
      <table className="tbl" style={{ border: "none" }}><thead><tr><th>Vendor</th><th>Spend</th></tr></thead>
        <tbody>{Object.entries(byV).sort((a, b) => b[1] - a[1]).map(([vid, amt]) => <tr key={vid}><td>{vName[vid] || "No vendor"}</td><td className="fig">{money(amt)}</td></tr>)}</tbody></table>
    </div>)}
  </div>);
}

function UsageReport({ usage }) {
  if (!usage.rows.length) return <div className="note">Weekly food cost activates once items have <b>two counts</b> logged (it's prior count + received − current count, valued at your cost). Keep counting and this fills in automatically.</div>;
  const [a, b] = usage.interval || [];
  return (<div>
    <div style={{ fontFamily: "'Barlow Condensed',sans-serif", fontWeight: 700, fontSize: 22, marginBottom: 4 }}>{money(usage.totalCost)} <span style={{ fontSize: 13, color: "#71757E" }}>cost of goods used</span></div>
    <div className="stat" style={{ marginBottom: 10 }}>between {a} and {b}, most recent count interval</div>
    <table className="tbl" style={{ border: "none" }}><thead><tr><th>Item</th><th>Used</th><th>Cost</th></tr></thead>
      <tbody>{usage.rows.slice(0, 40).map((r) => <tr key={r.id}><td>{r.name}</td><td className="fig">{r.used} {r.unit}</td><td className="fig">{r.cost != null ? money(r.cost) : "—"}</td></tr>)}</tbody></table>
  </div>);
}

function WeeklyReviewReport({ counts, receipts, products, onOpen }) {
  const r1 = (n) => Math.round(n * 10) / 10;
  const wkk = (d) => weekStart(d.slice(0, 10)).toISOString().slice(0, 10);
  const byItem = {}; const weekset = new Set();
  for (const c of counts) {
    const w = wkk(c.counted_at); weekset.add(w);
    const it = (byItem[c.product_id] ||= {}); const wo = (it[w] ||= {});
    const t = new Date(c.counted_at).getTime();
    if (!wo[c.location_id] || t >= wo[c.location_id].t) wo[c.location_id] = { qty: Number(c.qty), t };
  }
  const weeks = [...weekset].sort();
  if (!weeks.length) return <div className="note">No counts logged yet. Once you count two weeks in a row, last vs this vs used shows here.</div>;
  const thisW = weeks[weeks.length - 1];
  const lastW = weeks.length > 1 ? weeks[weeks.length - 2] : null;
  const recv = {};
  for (const r of receipts) {
    if (wkk(r.received_date) !== thisW) continue;
    for (const l of (r.receipt_line || [])) recv[l.product_id] = (recv[l.product_id] || 0) + (Number(l.qty_count_units) || Number(l.purchase_qty) || 0);
  }
  const total = (it, w) => (w && it[w]) ? Object.values(it[w]).reduce((a, x) => a + x.qty, 0) : null;
  const byId = Object.fromEntries(products.map((p) => [p.product_id, p]));
  const rows = Object.keys(byItem).map((pid) => {
    const it = byItem[pid]; const last = total(it, lastW); const cur = total(it, thisW); const rec = recv[pid] || 0;
    const used = (last != null && cur != null) ? last + rec - cur : null;
    return { pid, p: byId[pid], last, rec, cur, used };
  }).filter((r) => r.p && (r.last != null || r.cur != null)).sort((a, b) => a.p.name.localeCompare(b.p.name));
  const label = (w) => w ? new Date(w + "T00:00:00").toLocaleDateString(undefined, { month: "short", day: "numeric" }) : "—";
  return (
    <div>
      <div className="stat" style={{ marginBottom: 8 }}>Tap any row to see and fix that item's individual counts. Used = last count + received − this count.{lastW ? "" : " Count a second week to show the change."} A red Used (negative) usually means a delivery wasn't logged in Receive, or a miscount.</div>
      <div style={{ overflowX: "auto" }}>
        <table className="tbl" style={{ border: "none", minWidth: 440 }}>
          <thead><tr>
            <th>Item</th>
            <th style={{ textAlign: "right" }}>Last · {label(lastW)}</th>
            <th style={{ textAlign: "right" }}>+ Recv</th>
            <th style={{ textAlign: "right" }}>This · {label(thisW)}</th>
            <th style={{ textAlign: "right" }}>= Used</th>
          </tr></thead>
          <tbody>{rows.map((r) => (
            <tr key={r.pid} style={{ cursor: "pointer" }} onClick={() => onOpen && onOpen(r.p)}>
              <td><span style={{ borderBottom: "1px dashed #B7BBC4" }}>{r.p.name}</span><div className="stat">{r.p.count_unit}</div></td>
              <td className="fig" style={{ textAlign: "right" }}>{r.last == null ? "—" : r1(r.last)}</td>
              <td className="fig" style={{ textAlign: "right", color: r.rec ? "#0a5c50" : "#B7BBC4" }}>{r.rec ? r1(r.rec) : "—"}</td>
              <td className="fig" style={{ textAlign: "right" }}>{r.cur == null ? "—" : r1(r.cur)}</td>
              <td className="fig" style={{ textAlign: "right", fontWeight: 600, color: r.used == null ? "#B7BBC4" : r.used < 0 ? "#E0392B" : "#191B1F" }}>{r.used == null ? "—" : r1(r.used)}</td>
            </tr>
          ))}</tbody>
        </table>
      </div>
    </div>
  );
}

function ItemHistory({ product, onClose, onChanged }) {
  const [rows, setRows] = useState(null);
  const [busy, setBusy] = useState(false);
  const cpc = product.count_per_case || 1;
  async function load() { try { setRows(await db.getItemCounts(product.product_id)); } catch { setRows([]); } }
  useEffect(() => { load(); }, []);
  const setField = (id, f, v) => setRows((rs) => rs.map((r) => r.stock_count_id === id ? { ...r, [f]: v } : r));
  async function save(r) {
    setBusy(true);
    const cases = Number(r.cases) || 0, loose = Number(r.loose) || 0;
    try { await db.updateCount(r.stock_count_id, { cases, loose, qty: cases * cpc + loose }); await load(); onChanged && onChanged(); }
    catch (e) { alert("Save failed: " + (e.message || e)); }
    finally { setBusy(false); }
  }
  async function del(r) {
    if (!confirm("Delete this count entry?")) return;
    setBusy(true);
    try { await db.deleteCount(r.stock_count_id); await load(); onChanged && onChanged(); }
    catch (e) { alert("Delete failed: " + (e.message || e)); }
    finally { setBusy(false); }
  }
  return (
    <div className="overlay" onClick={onClose}>
      <div className="sheet" onClick={(e) => e.stopPropagation()}>
        <h2>{product.name}</h2>
        <div className="stat" style={{ marginBottom: 10 }}>1 case = {cpc} {product.count_unit}. Fix a wrong count by editing cases / loose, or delete the entry. The total recalculates automatically and on-hand updates.</div>
        {rows === null ? <div className="empty">Loading…</div> : rows.length === 0 ? <div className="note">No counts recorded for this item yet.</div> : (
          <div>
            {rows.map((r) => (
              <div className="crow" key={r.stock_count_id} style={{ gridTemplateColumns: "1fr 64px 64px 56px auto" }}>
                <div><b>{new Date(r.counted_at).toLocaleDateString(undefined, { month: "short", day: "numeric" })}</b><div className="stat">{r.location_name || "?"}</div></div>
                <label>Cases<input className="fig" type="number" value={r.cases} onChange={(e) => setField(r.stock_count_id, "cases", e.target.value)} /></label>
                <label>Loose<input className="fig" type="number" value={r.loose} onChange={(e) => setField(r.stock_count_id, "loose", e.target.value)} /></label>
                <div className="ctotal">= {(Number(r.cases) || 0) * cpc + (Number(r.loose) || 0)}</div>
                <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                  <button className="mini" disabled={busy} onClick={() => save(r)}>Save</button>
                  <button className="mini mini-danger" disabled={busy} onClick={() => del(r)}>✕</button>
                </div>
              </div>
            ))}
          </div>
        )}
        <button className="btn btn-ghost" style={{ marginTop: 14 }} onClick={onClose}>Close</button>
      </div>
    </div>
  );
}

function CountsByWeekReport({ counts, products, onOpen }) {
  if (!counts.length) return <div className="note">No counts logged yet. Once you count, each week's totals show here side by side so you can spot jumps.</div>;
  const wk = (d) => weekStart(d.slice(0, 10)).toISOString().slice(0, 10);
  const byItem = {}; const weekset = new Set();
  for (const c of counts) {
    const w = wk(c.counted_at); weekset.add(w);
    const it = (byItem[c.product_id] ||= {});
    const wo = (it[w] ||= {});
    const t = new Date(c.counted_at).getTime();
    if (!wo[c.location_id] || t >= wo[c.location_id].t) wo[c.location_id] = { qty: Number(c.qty), t };
  }
  const weekList = [...weekset].sort().slice(-8);
  const byId = Object.fromEntries(products.map((p) => [p.product_id, p]));
  const rows = Object.keys(byItem).map((pid) => ({
    pid, p: byId[pid],
    cells: weekList.map((w) => byItem[pid][w] ? Object.values(byItem[pid][w]).reduce((a, x) => a + x.qty, 0) : null),
  })).filter((r) => r.p).sort((a, b) => a.p.name.localeCompare(b.p.name));
  const label = (w) => new Date(w + "T00:00:00").toLocaleDateString(undefined, { month: "short", day: "numeric" });
  return (
    <div>
      <div className="stat" style={{ marginBottom: 8 }}>On-hand counted each week (Mon–Sun), latest count per location. A jump up usually means a delivery wasn't received in the app; a missing cell means it wasn't counted that week.</div>
      <div style={{ overflowX: "auto" }}>
        <table className="tbl" style={{ border: "none", minWidth: 360 }}>
          <thead><tr><th>Item</th>{weekList.map((w) => <th key={w} style={{ textAlign: "right" }}>{label(w)}</th>)}</tr></thead>
          <tbody>{rows.map((r) => (
            <tr key={r.pid} style={{ cursor: onOpen ? "pointer" : "default" }} onClick={() => onOpen && onOpen(r.p)}>
              <td>{onOpen ? <span style={{ borderBottom: "1px dashed #B7BBC4" }}>{r.p.name}</span> : r.p.name}<div className="stat">{r.p.count_unit}</div></td>
              {r.cells.map((c, i) => {
                const prev = i > 0 ? r.cells[i - 1] : null;
                const up = c != null && prev != null && c > prev;
                return <td key={i} className="fig" style={{ textAlign: "right", color: c == null ? "#B7BBC4" : up ? "#0a5c50" : "#191B1F" }}>{c == null ? "—" : c}</td>;
              })}
            </tr>
          ))}</tbody>
        </table>
      </div>
    </div>
  );
}

function CompletenessReport({ products }) {
  const noPrice = products.filter((p) => !(p.vendors || []).some((v) => v.price != null));
  const noCat = products.filter((p) => !p.category);
  const noVendor = products.filter((p) => !(p.vendors || []).length);
  const needsCount = products.filter((p) => p.count_unit === "each" && (p.count_per_case == p.pack));
  const block = (title, list) => (
    <div style={{ marginBottom: 12 }}>
      <div style={{ display: "flex", justifyContent: "space-between" }}><b>{title}</b><span className="fig" style={{ color: list.length ? "#E0392B" : "#0E7C6B" }}>{list.length}</span></div>
      {list.length > 0 && <div className="stat" style={{ marginTop: 3 }}>{list.slice(0, 12).map((p) => p.name).join(", ")}{list.length > 12 ? `, +${list.length - 12} more` : ""}</div>}
    </div>
  );
  return (<div>
    <div className="stat" style={{ marginBottom: 12 }}>Items that still need a detail filled in before the numbers are fully reliable.</div>
    {block("No vendor price", noPrice)}
    {block("No category", noCat)}
    {block("No vendor linked", noVendor)}
    {block("Count conversion still default", needsCount)}
  </div>);
}

function MenuReport({ products }) {
  const groups = { everyday: [], events: [], other: [] };
  for (const p of products) (groups[p.menu === "everyday" ? "everyday" : p.menu === "events" ? "events" : "other"]).push(p);
  const col = (title, list) => (
    <div className="vgroup" style={{ flex: 1, minWidth: 220 }}>
      <div className="vgroup-h"><span className="vname">{title}</span><span className="stat">{list.length}</span></div>
      <div style={{ padding: "8px 0", fontSize: 13 }}>{list.slice(0, 30).map((p) => <div key={p.product_id} style={{ padding: "2px 0" }}>{p.name}{p.menu !== "everyday" && p.menu !== "events" ? <span className="bchip" style={{ marginLeft: 6 }}>{p.menu}</span> : null}</div>)}{list.length > 30 ? <div className="stat">+{list.length - 30} more</div> : null}</div>
    </div>
  );
  return <div style={{ display: "flex", gap: 12, flexWrap: "wrap" }}>{col("Everyday", groups.everyday)}{col("Events", groups.events)}{groups.other.length ? col("Special / Vending", groups.other) : null}</div>;
}
